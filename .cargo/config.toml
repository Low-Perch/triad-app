# getrandom 0.3 needs an explicit backend opt-in on wasm (the "wasm_js"
# cargo feature alone is not enough) — used by random-game generation
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
